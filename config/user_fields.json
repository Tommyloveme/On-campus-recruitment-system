{
  "comment": "用户附属信息字段配置：唯一性由工号(username)决定，其余均为附属信息。管理员可在本文件新增字段并定义其操作模式（text 自由输入 / select 单选下拉，options 为可选项）。builtin 字段对应 users 表内置列；非 builtin 字段存入 users.extra(JSON)。display_name 始终必填且须中文。",
  "fields": [
    {
      "key": "display_name",
      "label": "姓名",
      "type": "text",
      "required": true,
      "builtin": true,
      "pattern": "chinese"
    },
    {
      "key": "supervisor",
      "label": "主管",
      "type": "text",
      "required": true,
      "builtin": true,
      "pattern": "chinese"
    },
    {
      "key": "dept_level2",
      "label": "二层部门",
      "type": "select",
      "options": [
        "软件部",
        "模型部",
        "仿真部",
        "测试部"
      ],
      "required": true,
      "builtin": true
    },
    {
      "key": "dept_level3",
      "label": "三层部门",
      "type": "text",
      "required": false,
      "builtin": true
    },
    {
      "key": "pl_group",
      "label": "PL组",
      "type": "text",
      "required": false,
      "builtin": true
    }
  ]
}